from typing import Dict, Any
from bpm.utils.ui.console import BPMConsole

console = BPMConsole()

def generate_nextflow_config(inputs: Dict[str, Any]) -> None:
    """Generate the nextflow config file."""
    pass